# Synthetic live-ledger test key

This scope and its keys exist only to test the public anchoring workflow. They
must never sign a calibration or held-out evaluation record. Both private keys
were generated outside the repository in disposable local storage.
